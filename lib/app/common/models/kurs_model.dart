class KursModel {
  int id;
  double currency;
  int userId;
  DateTime createdAt;

  KursModel({
    required this.id,
    required this.currency,
    required this.userId,
    required this.createdAt,
  });
}
